// Copyright 2017 Karthik

#include <stdio.h>

int main(void) {
  int n;

  for (;;) {
    printf("enter a number: ");
    scanf("%d", &n);
    if ( n == 0)
      break;
    printf("%d cubes is %d\n", n, n * n * n);
  }
  
  return 0;
}
